<div class="container">
  <div class="row">
    <div class="col-4">
      <%= render 'public/users/info', user: current_user %>
      <%= render 'public/books/form', book: @book %>
    </div>
    <div class="col-8">
      <% @books.each do |book| %>
      <%= book.name %>
      <%= book.body %>
      <% end %>
    </div>
  </div>
</div>
